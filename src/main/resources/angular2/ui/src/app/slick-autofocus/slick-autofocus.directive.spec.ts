import { SlickAutofocusDirective } from './slick-autofocus.directive';

describe('SlickAutofocusDirective', () => {
  it('should create an instance', () => {
    const directive = new SlickAutofocusDirective();
    expect(directive).toBeTruthy();
  });
});
